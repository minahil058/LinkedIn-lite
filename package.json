{
  "name": "job-portal-saas",
  "version": "1.0.0",
  "description": "Professional Job Portal SaaS - Unified Deployment",
  "main": "api/index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "backend-install": "cd backend && npm install",
    "install-all": "npm install && npm run backend-install && cd frontend && npm install",
    "start": "node backend/server.js",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": ">=18.x"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",
    "postcss": "^8.5.10",
    "tailwindcss": "^4.2.4"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.9.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "datauri": "^4.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.5.0",
    "morgan": "^1.10.1",
    "multer": "^1.4.5-lts.1",
    "redux-persist": "^6.0.0"
  }
}
